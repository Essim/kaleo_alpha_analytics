{
  "name": "kaleotopia-alpha-analytics-visualizer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build:data": "ruby scripts/build_data.rb",
    "dev": "wrangler dev --local --port 8787",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "wrangler": "^4.0.0"
  }
}
